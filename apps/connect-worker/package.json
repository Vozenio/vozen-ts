{
  "name": "vozen-connect-worker",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:remote": "wrangler d1 migrations apply vozen-connect --remote",
    "db:migrate:local": "wrangler d1 migrations apply vozen-connect --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
